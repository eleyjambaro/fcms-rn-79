import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const AppPreviouslyInstalledScreen = () => {
  return (
    <View>
      <Text>AppPreviouslyInstalledScreen</Text>
    </View>
  );
};

export default AppPreviouslyInstalledScreen;

const styles = StyleSheet.create({});
